import styled from "styled-components";

export const Button = styled.button`
	background-color: #12bd48;
	color: #fff;
	border-radius: 5px;
	border: none;
	flex: 1;
	padding: 5px 15px;
	cursor: pointer;
	box-shadow: 4px 4px 4px rgba(0, 100, 0, 0.5);

	&:hover {
		opacity: 0.7;
	}
`;
